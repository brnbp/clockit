const { expect } = require('chai');

const { start, lunchIn, lunchOut, end, status, clearToday } = require('../../../src/clockit/clockit');

describe('Clockit: start', () => {
  it('Should start', () => {
    
  });
});
